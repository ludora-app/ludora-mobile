import React from 'react'
import { Button } from '@ludo/ui'

export default function PlayersListItemInvite() {
  return (
    <Button
      size="sm"
      title="Inviter"
      iconProps={{ name: 'add-circle-regular', position: 'left' }}
      onPress={() => { }}
    />
  )
}