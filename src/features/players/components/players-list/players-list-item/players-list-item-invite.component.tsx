import React from 'react'
import { Button } from '@ludo/ui'

export default function PlayersListItemInvite() {
  return (
    <Button
      size="sm"
      title="Inviter"
      iconProps={{ className: "mr-2", name: "user-add-solid", position: "left" }}
      onPress={() => { }}
    />
  )
}